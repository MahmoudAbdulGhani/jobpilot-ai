'use client';
import { FormEvent,useState } from 'react';import { Compass } from '@phosphor-icons/react';import { useAuth } from '../../lib/auth';
export default function Login(){const{signIn,loading}=useAuth();const[pending,setPending]=useState(false);const[error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();if(pending)return;const d=new FormData(e.currentTarget);setPending(true);setError('');try{await signIn(String(d.get('email')),String(d.get('password')))}catch(x){setError(x instanceof Error?x.message:'Unable to sign in');setPending(false)}}if(loading)return <main className="center-state">Restoring your session…</main>;return <main className="login-page"><section className="login-card"><div className="brand"><Compass size={42} weight="duotone"/>JobPilot AI</div><p className="eyebrow">Your career journal</p><h1>Welcome back</h1><p className="muted">Sign in to continue to your saved opportunities.</p><form onSubmit={submit}><label>Email<input name="email" type="email" autoComplete="username" required autoFocus/></label><label>Password<input name="password" type="password" autoComplete="current-password" required/></label>{error&&<p className="form-error" role="alert">{error}</p>}<button disabled={pending} className="primary-button">{pending?'Signing in…':'Sign in'}</button></form></section></main>}
